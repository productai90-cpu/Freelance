<div dir="rtl">

# راهنمای انتشار سایت روی GitHub Pages

آدرس سایت: **https://productai90-cpu.github.io/Freelance/**

---

## چرا روش قبلی شما اینجا جواب نمی‌داد

روش قدیمی شما «Add files via upload» بود — کشیدن فایل‌ها داخل صفحهٔ گیت‌هاب. برای یک سایت ساده که از HTML و CSS خام ساخته شده، آن روش کاملاً درست است: فایلی که آپلود می‌کنید همان فایلی است که مرورگر بازدیدکننده اجرا می‌کند.

**ولی این پروژه اینطور نیست.** کدی که در `src/` نوشته شده مستقیماً در هیچ مرورگری اجرا نمی‌شود:

| چیزی که در `src/` هست | چیزی که مرورگر می‌فهمد |
|---|---|
| `.jsx` — نحو React | فقط جاوااسکریپت ساده |
| کلاس‌های Tailwind | فقط CSS واقعی |
| `import` بین ده‌ها فایل | یک باندل بهینه‌شده |
| `hero.webp` بدون هش | فایل هش‌دار برای کش مرورگر |

بین این دو ستون یک مرحله به اسم **build** لازم است. دستور `npm run build` این کار را می‌کند و خروجی را در پوشهٔ `dist/` می‌گذارد.

پس اگر با روش قدیمی `src/` را آپلود می‌کردید، بازدیدکننده صفحهٔ سفید می‌دید. تنها راه دستی این بود که هر بار خودتان build بگیرید و **فقط** محتوای `dist/` را آپلود کنید — که سه ایراد دارد:

۱. هر تغییر کوچک یعنی build دستی + آپلود دستی
۲. کد اصلی هیچ‌وقت روی گیت‌هاب نمی‌رود، پس تاریخچه و نسخه‌بندی ندارید
۳. `dist/` عمداً در `.gitignore` است چون خروجی تولیدی است، نه منبع

---

## روشی که الان کار می‌کند

به‌جای آپلود خروجی، **کد اصلی** را push می‌کنید و گیت‌هاب خودش build می‌گیرد.

```
شما: git push  ──▶  GitHub Actions
                      ├─ npm ci          نصب پکیج‌ها
                      ├─ npm run build   ساخت dist/
                      ├─ upload artifact
                      └─ deploy ──▶ سایت زنده
```

دستورش در فایل `.github/workflows/deploy.yml` است و روی هر push به شاخهٔ `main` خودکار اجرا می‌شود.

| | آپلود دستی | GitHub Actions |
|---|---|---|
| چه چیزی آپلود می‌شود | فایل نهایی | کد اصلی |
| build را کی می‌گیرد | خودتان | سرور گیت‌هاب |
| تکرارپذیری | هر بار دستی | خودکار |
| تاریخچهٔ کد | ندارد | کامل |
| برگشت به نسخهٔ قبل | ممکن نیست | یک دستور |

---

## مراحلی که طی شد

### ۱. تنظیم هویت git

```bash
git config user.email "productai90@gmail.com"
git config user.name  "productai90-cpu"
```

بدون این، دستور `commit` با پیام `Author identity unknown` شکست می‌خورد.

### ۲. تغییر نام شاخه

```bash
git branch -M main
```

شاخهٔ محلی `master` بود ولی ورک‌فلو فقط به `main` گوش می‌دهد:

```yaml
on:
  push:
    branches: [main]
```

اگر این را عوض نمی‌کردیم، push انجام می‌شد ولی **هیچ اتفاقی نمی‌افتاد** — نه خطایی، نه دیپلویی.

### ۳. جدا کردن فایل‌های خصوصی

در `.gitignore`:

```
node_modules      وابستگی‌ها — با npm ci دوباره ساخته می‌شوند
dist              خروجی build — گیت‌هاب خودش می‌سازد
New images/       ۱۴ مگابایت عکس خام، قبلاً به webp تبدیل شده
MENU.md           فرم کاری، نه کد سایت
_TEMPLATE-*.md    قالب‌های داخلی
*.pdf             خروجی تولیدی
Talar.txt         یادداشت‌های فروش
*نکات*.md         یادداشت‌های مذاکره
```

### ۴. اتصال و ارسال

```bash
git remote add origin https://github.com/productai90-cpu/Freelance.git
git add -A
git status                        # ← همیشه قبل از commit نگاه کنید
git commit -m "پیام توضیحی"
git push --force -u origin main
```

`--force` فقط این یک بار لازم بود، چون مخزن تاریخچهٔ بی‌ربط قبلی را داشت. **از این به بعد هرگز از آن استفاده نکنید** — `git push` ساده کافی است.

### ۵. فعال کردن Pages

**Settings ← Pages ← Source: GitHub Actions**

این مرحله را حتماً باید یک بار دستی انجام دهید.

---

## دو نکته‌ای که وقتمان را گرفت

**«Actions» دو جای متفاوت است.**

- **تب Actions** در نوار بالای مخزن → جایی که اجراها را می‌بینید و دکمهٔ Run workflow آنجاست
- **Settings ← Actions** → تنظیمات مجوز اجراکننده‌ها، ربطی به انتشار ندارد

**ترتیب مهم است.** دو اجرای اول در مرحلهٔ `configure-pages` شکست خوردند، چون Pages هنوز فعال نشده بود. بعد از فعال کردنش هم چیزی منتشر نشد، چون push جدیدی نبود که ورک‌فلو را راه بیندازد. راه‌حل: تب Actions ← Deploy to GitHub Pages ← **Run workflow**.

---

## از این به بعد

هر تغییری که دادید:

```bash
git add -A
git commit -m "توضیح کوتاه تغییر"
git push
```

تمام. حدود دو دقیقه بعد سایت به‌روز است. وضعیت را در تب **Actions** می‌بینید — سبز یعنی منتشر شد.

### اگر خطا خورد

در تب Actions روی اجرای قرمز کلیک کنید و مرحلهٔ ✗ را باز کنید. رایج‌ترین‌ها:

| خطا | معنی |
|---|---|
| `npm ci` شکست خورد | `package-lock.json` با `package.json` هماهنگ نیست — یک بار `npm install` بزنید و lock را کامیت کنید |
| `npm run build` شکست خورد | خطای واقعی در کد. همان خطا را با `npm run build` روی سیستم خودتان هم می‌بینید |
| `configure-pages` شکست خورد | Pages فعال نیست — Settings ← Pages |

### قبل از هر push

```bash
npm run build     # اگر اینجا سبز شد، روی گیت‌هاب هم سبز می‌شود
git status        # ببینید چه چیزی قرار است برود
```

</div>
